# Generated by `cplt init` — review before trusting.
# Detected: JVM (Gradle), Docker, Ktor, TestContainers, Flyway
# Workspace members:
#   ktlint-rules (settings.gradle) — JVM (Gradle)
#   brevbaker/core (settings.gradle) — JVM (Gradle), Ktor, TestContainers
#   brevbaker/brevbaker-api (settings.gradle) — JVM (Gradle)
#   brevbaker/brevdata (settings.gradle) — JVM (Gradle)
#   brevbaker/serialization (settings.gradle) — JVM (Gradle)
#   brevbaker/dsl (settings.gradle) — JVM (Gradle)
#   brevbaker/pdf-bygger (settings.gradle) — JVM (Gradle), Docker, Ktor, TestContainers
#   brevbaker/template-model-generator (settings.gradle) — JVM (Gradle)
#   skribenten-backend (settings.gradle) — JVM (Gradle), Docker, Ktor, TestContainers, Flyway
#   pensjon/brevbaker (settings.gradle) — JVM (Gradle), Docker, Ktor
#   pensjon/maler (settings.gradle) — JVM (Gradle)
#   pensjon/pensjon-api-model (settings.gradle) — JVM (Gradle)
#   ufoere/maler (settings.gradle) — JVM (Gradle)
#   ufoere/ufoere-api-model (settings.gradle) — JVM (Gradle)
#   alder/maler (settings.gradle) — JVM (Gradle)
#   alder/alder-api-model (settings.gradle) — JVM (Gradle)
#   etterlattemaler (settings.gradle) — JVM (Gradle)
#   planlegge-pensjon-maler (settings.gradle) — JVM (Gradle)

[propose]
# ⚠️  grants access to Docker socket — effectively root on the host
# Suggested by: (root), brevbaker/core, brevbaker/pdf-bygger, pensjon/brevbaker, skribenten-backend
allow_docker = true
# Suggested by: (root), alder/alder-api-model, alder/maler, brevbaker/brevbaker-api, brevbaker/brevdata, brevbaker/core, brevbaker/dsl, brevbaker/pdf-bygger, brevbaker/serialization, brevbaker/template-model-generator, etterlattemaler, ktlint-rules, pensjon/brevbaker, pensjon/maler, pensjon/pensjon-api-model, planlegge-pensjon-maler, skribenten-backend, ufoere/maler, ufoere/ufoere-api-model
allow_jvm_attach = true
# Suggested by: brevbaker/core, brevbaker/pdf-bygger, skribenten-backend
allow_localhost_any = true

[propose.allow]
ports = [5015, 5016, 5017, 5432, 8080, 8081, 8082, 8083, 8084, 8088, 8089, 20080]
localhost = [8080]

[propose.proxy]
allow_private_domains = ["mimir.nav.cloud.nais.io", "loki.nav.cloud.nais.io", "tempo.prod-gcp.nav.cloud.nais.io"]
