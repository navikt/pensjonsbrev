#Used for local testing purposes
services:
  pdf-bygger:
    build: ./pdf-bygger
    ports:
      - "8081:8080"
  pensjon-brevbaker:
    environment:
      - PDF_BYGGER_URL=http://pdf-bygger:8080
    build: ./pensjon-brevbaker
    ports:
      - "8080:8080"