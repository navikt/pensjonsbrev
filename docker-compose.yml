#Used for local testing purposes
services:
  pdf-bygger:
    build: ./pdf-bygger
    tmpfs:
      - /app/pdfcompilation
    ports:
      - "8081:8080"
  pensjon-brevbaker:
    build: ./pensjon-brevbaker
    ports:
      - "8080:8080"