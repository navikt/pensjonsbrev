name: "verify: brevoppskrift-web"

on:
  pull_request:
    branches:
      - 'main'
    paths:
      - .github/workflows/verify-brevoppskrift-web.yaml
      - brevoppskrift-web/**

permissions:
  contents: read

jobs:
  verify:
    uses: ./.github/workflows/.verify-frontend.yaml
    secrets: inherit
    with:
      workingDir: brevoppskrift-web
      cypress: false