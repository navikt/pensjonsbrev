name: "Build and deploy pdf-bygger"
on:
  workflow_dispatch:
  pull_request:
    paths:
      - "build.gradle.kts"
      - "gradle.properties"
      - "gradle/libs.versions.toml"
      - "brevbaker/pdf-bygger/**"
      - ".github/workflows/.build-backend.yaml"
      - ".github/workflows/pdfbygger.yml"
      - ".github/workflows/cleanup.yml"
      - "gradle/wrapper/gradle-wrapper.jar"
      - "gradle/wrapper/gradle-wrapper.properties"
      - "!**/**.md"
  push:
    branches:
      - "main"
    paths:
      - "build.gradle.kts"
      - "gradle.properties"
      - "gradle/libs.versions.toml"
      - "brevbaker/pdf-bygger/**"
      - ".github/workflows/.build-backend.yaml"
      - ".github/workflows/pdfbygger.yml"
      - ".github/workflows/cleanup.yml"
      - "gradle/wrapper/gradle-wrapper.jar"
      - "gradle/wrapper/gradle-wrapper.properties"
      - "!**/**.md"
  # Vi må kjøre Percy minst hver 30. dag for å ha en baseline for visuelle tester. Disse planlagte kjøringene er for det formålet.
  schedule:
    - cron: "0 5 1,15 * *"
env:
  "IMAGE_PDF_BYGGER": "ghcr.io/${{ github.repository }}/pdf-bygger:${{ github.sha }}"
  "IMAGE_BRANCH": "ghcr.io/${{ github.repository }}/pdf-bygger:main"
  JavaDistribution: "temurin"
  JavaVersion: 25

permissions:
  contents: read
  packages: read
  id-token: write

jobs:
  buildPdfbygger:
    uses: ./.github/workflows/.build-backend.yaml
    secrets: inherit
    with:
      image_suffix: pdf-bygger
      path: brevbaker/pdf-bygger
      target: pdf-bygger
      gradleTarget: brevbaker:pdf-bygger
      buildImage: true
      uploadJar: true

  pushPdfByggerToGithubRegistry:
    name: "Push pdf-bygger to github registry"
    needs: buildPdfbygger
    runs-on: ubuntu-latest
    permissions:
      packages: "write"
      id-token: "write"
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Download jar
        uses: actions/download-artifact@v7
        with:
          name: pdf-bygger-jar
          path: brevbaker/pdf-bygger/build/install/pdf-bygger/lib
      - name: Docker login
        uses: docker/login-action@v3
        with:
          registry: "https://ghcr.io"
          username: ${{ github.repository }}
          password: ${{ github.token }}
      - name: "Build Docker image"
        run: | 
          docker build --pull --tag ${IMAGE_PDF_BYGGER} brevbaker/pdf-bygger
      - name: "Push image with sha"
        run: |
          docker push ${IMAGE_PDF_BYGGER}
      - name: "Tag image with main"
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
        run: |
          docker tag ${IMAGE_PDF_BYGGER} ${IMAGE_BRANCH}
      - name: "Push image with main"
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
        run: |
          docker push ${IMAGE_BRANCH}

  salsa-pdfbygger:
    uses: ./.github/workflows/.salsa.yaml
    needs: buildPdfbygger
    secrets: inherit
    with:
      digest: ${{ needs.buildPdfbygger.outputs.digest }}
      image: ${{ needs.buildPdfbygger.outputs.image }}

  changes:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      latex: ${{ steps.changes.outputs.latex }}
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36
        id: changes
        with:
          filters: |
            latex:
              - 'brevbaker/pdf-bygger/containerFiles/latex/**'
              - 'brevbaker/pdf-bygger/Dockerfile'

  runIntegrationTests:
    name: "Integration tests"
    needs: [ pushPdfByggerToGithubRegistry, changes ]
    runs-on: ubuntu-latest-16-cores
    permissions:
      statuses: write
      checks: write
      pull-requests: write
      packages: write
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: gradle/actions/setup-gradle@v5
        with:
          cache-encryption-key: ${{ secrets.GRADLEKEY }}
      - uses: actions/setup-java@v5
        with:
          "distribution": ${{ env.JavaDistribution }}
          "java-version": ${{ env.JavaVersion }}
      - name: "run integration tests"
        run: |
          ./gradlew :brevbaker:api-model-common:publishToMavenLocal 
          ./gradlew :brevbaker:pdf-bygger:integrationTest
        env:
          "PDF_BYGGER_IMAGE": "ghcr.io/navikt/pensjonsbrev/pdf-bygger:${{ github.sha }}"
      - name: Publish Test Report
        if: ${{ always() }}
        uses: scacap/action-surefire-report@5609ce4db72c09db044803b344a8968fd1f315da
        with:
          fail_if_no_tests: true
          fail_on_test_failures: true
          report_paths: '**/build/test-results/integrationTest/TEST-*.xml'
      - name: Upload images
        if: ${{ (needs.changes.outputs.latex == 'true' || (github.ref == 'refs/heads/main' && github.event_name == 'schedule')) }}
        uses: actions/upload-artifact@v6
        with:
          name: visuals
          path: brevbaker/pdf-bygger/build/test_visual/

  analyzeImages:
    name: "Analyze images"
    needs: [ runIntegrationTests, changes ]
    runs-on: ubuntu-latest-16-cores
    if: ${{ (needs.changes.outputs.latex == 'true' || (github.ref == 'refs/heads/main' && github.event_name == 'schedule')) }}
    permissions:
      statuses: write
      checks: write
      pull-requests: write
    steps:
      - name: Download visuals
        uses: actions/download-artifact@v7
        with:
          name: visuals
          path: brevbaker/pdf-bygger/build/test_visual/
      - name: Convert visual-test pdfs
        uses: routsi/ImageMagick-action@v1.1.1
        with:
          command: mogrify -path brevbaker/pdf-bygger/build/test_visual -format png -background white -alpha remove -alpha off -density 230 -quality 85 brevbaker/pdf-bygger/build/test_visual/pdf/*.pdf
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - run: npm install --save-dev @percy/cli
      - name: Publish visual-tests to percy
        run: |
          npx percy upload brevbaker/pdf-bygger/build/test_visual/ > percy.log
          cat percy.log
          URL=$(cat percy.log | grep -e "build #[0-9]\+:" | sed 's/^.*build #[0-9]\+: //g')
          if [[ $URL ]]; then echo $URL > percy-build-url;else echo "Percy upload feilet" > percy-build-url;fi
        env:
          PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}
      - name: Comment percy review link
        uses: peter-evans/create-or-update-comment@e8674b075228eee787fea43ef493e45ece1004c9
        if: ${{ needs.changes.outputs.latex == 'true' && github.event_name == 'pull_request' }}
        with:
          issue-number: ${{ github.event.number }}
          body-path: percy-build-url


  deployPdfByggerToDev:
    name: "Deploy pdf-bygger to dev"
    uses: ./.github/workflows/.deploy.yaml
    secrets: inherit
    needs: [runIntegrationTests, buildPdfbygger]
    with:
      cluster: "dev-gcp"
      resource: "brevbaker/pdf-bygger/.nais/nais.yaml,brevbaker/pdf-bygger/.nais/hpa.yaml"
      image: ${{needs.buildPdfbygger.outputs.image}}
      vars: "brevbaker/pdf-bygger/.nais/dev.yaml"

  deployPdfByggerToProd:
    name: "Deploy pdf-bygger to prod"
    uses: ./.github/workflows/.deploy.yaml
    secrets: inherit
    needs: [runIntegrationTests, buildPdfbygger]
    with:
      cluster: "prod-gcp"
      resource: "brevbaker/pdf-bygger/.nais/nais.yaml,brevbaker/pdf-bygger/.nais/hpa.yaml"
      image: ${{needs.buildPdfbygger.outputs.image}}
      vars: "brevbaker/pdf-bygger/.nais/prod.yaml"

  "klarTilMerge":
    name: "Klar til merge"
    needs: [buildPdfbygger, pushPdfByggerToGithubRegistry, runIntegrationTests]
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: ./.github/pr